// Importing all of the contents of the file could be done with the import keyword followed by the *.
// You could import the contents into an object with the (as) keyword. 

"use strict";
import * as capitalize_strings from "capitalize_strings"