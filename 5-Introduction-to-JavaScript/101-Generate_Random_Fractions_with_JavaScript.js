// .random() is a method of the Math object that returns a random decimal between 0 (inclusive) and 1 (exclusive).

function randomFraction() {

    // Only change code below this line.

    return Math.random();

    // Only change code above this line.
}