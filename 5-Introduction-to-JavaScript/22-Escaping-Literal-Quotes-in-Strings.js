// To escape single or double quotes in JS, one could add a "\" in front of the 
// quotes to signal that it is not the end of the string. 

var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line
