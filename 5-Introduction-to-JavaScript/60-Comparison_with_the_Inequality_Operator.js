// The inequality operator (!=) will evaluate the statement and determine if the statement is not equal. It does not take the data type into consideration when evaluating the statement. 

// Setup
function testNotEqual(val) {
    if (val!=99) { // Change this line
      return "Not Equal";
    }
    return "Equal";
  }
  
  // Change this value to test
  testNotEqual(99);