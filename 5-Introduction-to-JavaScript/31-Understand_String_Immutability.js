// Strings are immutable and cannot be changed or altered. If we would like to alter a variable holding a string, we would have to assign it a completely new string.

// Setup
var myStr = "Jello World";

// Only change code below this line

myStr = "Hello World"; // Fix Me