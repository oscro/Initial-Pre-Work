// .pop() is a method that removes the final entry of an array and returns the entry.
// In the example below, .pop() was used to remove myArray[1] from myArray and stored within removedFromMyArray.

// Example
var ourArray = [1,2,3];
var removedFromOurArray = ourArray.pop(); 
// removedFromOurArray now equals 3, and ourArray now equals [1,2]

// Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line.
var removedFromMyArray = myArray.pop();