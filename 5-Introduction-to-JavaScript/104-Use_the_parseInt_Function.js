// parseInt() is a function that will take a given argument and convert it into an integer or NaN (Not a Number).

function convertToInteger(str) {
    console.log(parseInt(str));
    return parseInt(str);
}

convertToInteger("56");