// The ternary operator allows for a one line if/else expression. It is as follows, (condition) ? (do this if statement is true) : (do this if statement is false);

function checkEqual(a, b) {
    return a == b ? true : false;
}

checkEqual(1, 2);